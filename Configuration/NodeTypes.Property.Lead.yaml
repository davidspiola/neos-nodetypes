'Webandco.NodeTypes:Property.Lead':
  properties:
    lead:
      search:
        fulltextExtractor: ${Indexing.extractInto('text', value)}
      ui:
        inline:
          editorOptions:
            placeholder: i18n
      options:
        silhouette: 'webco.text.none'
