prototype(Webandco.NodeTypes:Property.Headline) < prototype(Neos.Fusion:Content) {

    headline = ${q(node).property('headline')}
    tagName = 'h2'

    renderer = Webandco.NodeTypes:Renderer.Headline {
        tagName = ${props.tagName}
        content = ${props.headline}
    }
}
