prototype(Webandco.NodeTypes:Property.Label) < prototype(Neos.Fusion:Content) {

    label = ${q(node).property('label')}
    tagName = 'span'

    renderer = Webandco.NodeTypes:Renderer.Label {
        tagName = ${props.tagName}
        content = ${props.label}
    }
}
