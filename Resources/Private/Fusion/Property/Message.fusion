prototype(Webandco.NodeTypes:Property.Message) < prototype(Neos.Fusion:Content) {

    text = ${q(node).property('message')}
    class = 'message'

    renderer = Webandco.NodeTypes:Renderer.Message {
        text = ${props.text}
        class = ${props.class}
    }
}
