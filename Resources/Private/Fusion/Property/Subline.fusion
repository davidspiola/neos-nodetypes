prototype(Webandco.NodeTypes:Property.Subline) < prototype(Neos.Fusion:Content) {

    subline = ${q(node).property('subline')}
    tagName = 'h2'

    renderer = Webandco.NodeTypes:Renderer.Subline {
        tagName = ${props.tagName}
        content = ${props.subline}
    }
}
