prototype(Webandco.NodeTypes:Property.Text) < prototype(Neos.Fusion:Content) {

    text = ${q(node).property('text')}
    class = 'text'
    modifier = ''

    renderer = Webandco.NodeTypes:Renderer.Text {
        text = ${props.text}
        class = ${props.class}
        modifier = ${props.modifier}
    }
}
